{% block form_errors %}
    {% spaceless %}
        {% if errors|length > 0 %}
            <div class="form__error">{{ errors[0].message }}</div>
        {% endif %}
    {% endspaceless %}
{% endblock form_errors %}